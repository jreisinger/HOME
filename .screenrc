# define a bigger scrollback, default is 100 lines
defscrollback 15000

# turn off sending of screen messages to hardstatus
hardstatus off

# a bar to display at the bottom
hardstatus alwayslastline "%?%-Lw%?%{bw}%n*%f %t%?(%u)%?%{wk}%?%+Lw%?"

startup_message off
